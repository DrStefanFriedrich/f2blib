/**
 * <b>Implementation note for the visitor tests in this package</b><p />
 * The tests in this package are not true unit tests in the narrow sense,
 * because they don't mock "back-ends". But they are fast, and for us
 * this is sufficient to call them unit test.
 */
package org.f2blib.visitor;
